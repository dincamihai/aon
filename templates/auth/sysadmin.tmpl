      ## ── sysadmin: bootstrap + ops only ─────────────────────────
      { user: sysadmin, password: "PASSWORD_SYSADMIN",
        permissions: {
          publish:   { allow: [">"] }
          subscribe: { allow: [">"] }
          allow_responses: true
        }
      }

