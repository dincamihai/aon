"""ai-over-nats engine — Python wrapper for the bash CLI.

Installed via `pipx install --editable <path-to-ai-over-nats>`. The
package itself is intentionally minimal: it locates the engine root
(where bin/aon, templates/, scripts/ live) and execs the bash CLI.
"""

__version__ = "0.1.0"
